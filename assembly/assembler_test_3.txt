        lw      0       1       num         ; โหลดค่าจากหน่วยความจำที่ label "num" (10) → r1
        lw      0       2       one         ; โหลดค่าจากหน่วยความจำที่ label "one" (1) → r2
        jalr    4       7                   ; กระโดดไปยังตำแหน่งใน r4 (ยังไม่ได้ตั้งค่า) และเก็บ return address ไว้ใน r7
        halt                                ; หยุดการทำงานของโปรแกรม
num     .fill   10                          ; เก็บค่าคงที่ 10 ในหน่วยความจำ (label num)
one     .fill   1                           ; เก็บค่าคงที่ 1 ในหน่วยความจำ (label one)
        noop                                ; ไม่ทำอะไร (เว้นบรรทัด)
        noop                                ; ไม่ทำอะไร (เว้นบรรทัด)
        noop                                ; ไม่ทำอะไร (เว้นบรรทัด)
subr    add     1       2       3           ; r3 = r1 + r2 (คำนวณผลรวม 10 + 1 = 11)
        beq     0       0       endSub      ; เงื่อนไขจริงเสมอ (0 == 0) → กระโดดไปยัง label endSub
        noop                                ; ไม่ทำอะไร (ช่องว่าง)
endSub  jalr    7       4                   ; กลับไปยังตำแหน่งที่อยู่ใน r7 (return ไปยัง caller)




